/*
 * Ensure the API_URL constant is updated to reflect the actual location
 * of the quotations web API. This string should include:
 *   - The protocol: "http://" or "https://"
 *   - The domain name or IP address: For example, "api.example.com" or
 *     "192.168.1.10"
 *   - The port number: Only if the API is not running on the default HTTP
 *     (port 80) or HTTPS (port 443) port. For example, ":8080"
 *   - Do NOT include a slash character (/) at the end of the URL
 */
const API_URL = 'https://xeenl4ane3lxuldmqksruvsjlq0urmgr.lambda-url.us-east-1.on.aws';

export default API_URL;
